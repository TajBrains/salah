.flex.items-center.justify-center
  .scaffold.w-max.text-left
    %h3.text-2xl.font-bold.text-center Change your password
    = form_for(resource, as: resource_name, url: password_path(resource_name), html: { "data-turbo": false, method: :put }) do |f|
      = render "devise/shared/error_messages", resource: resource
      = f.hidden_field :reset_password_token
      .mt-4
        %div
          = f.label :password, class: "block", for: "password"
          = f.password_field :password, autofocus: true, autocomplete: "new-password", class: "w-full px-4 py-2 mt-2 border rounded-md focus:outline-none focus:ring-1 focus:ring-blue-600", id: "password", placeholder: "New password"
        %div
          = f.label :password_confirmation, class: "block", for: "password_confirmation"
          = f.password_field :password_confirmation, autofocus: true, autocomplete: "new-password", class: "w-full px-4 py-2 mt-2 border rounded-md focus:outline-none focus:ring-1 focus:ring-blue-600", id: "password_confirmation", placeholder: "New password confirmation"
        .flex.items-baseline.justify-between
          = f.submit "Change my password", class: "px-6 py-2 mt-4 text-white bg-blue-600 rounded-lg hover:bg-blue-900", id: "submit"