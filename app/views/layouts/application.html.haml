!!!
%html
  %head
    %meta{content: "text/html; charset=UTF-8", "http-equiv" => "Content-Type"}
    %title SalahApp
    %meta{content: "width=device-width,initial-scale=1", name: "viewport"}
    = csrf_meta_tags
    = csp_meta_tag
    = stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload"
    = stylesheet_link_tag "application", "data-turbo-track": "reload"
    = javascript_importmap_tags
  %body{class: "min-w-full min-h-screen md:overflow-hidden", data: { controller: "cursor watcher", watcher_callback_url_value: root_url, cursor_target: "activityArea", watcher_times_value: @pray_time_service.times }, style: send("bg_image_#{@version}")}
    .mx-auto.px-6
      = yield
