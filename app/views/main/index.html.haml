.grid.grid-cols-12.gap-2.w-full
  .hidden.h-screen.flex-col.items-center.justify-around.py-4.md:flex.md:col-span-7

    .flex.flex-col.items-center.justify-around.space-y-6{ data: { controller: "current-time" } }
      .text-9xl.font-medium.text-white{ data: { current_time_target: "time" } }
        = Time.current.strftime("%H:%M:%S")
      .text-7xl.text-white.font-medium.tracking-wider
        = l(Date.current, format: :long, locale: :tr)
      .text-amber-400.text-5xl.font-bold
        = Date.current.to_hijri.strftime('%d %B, %Y')

    .flex.flex-col.items-center.justify-around.space-y-6{data: { controller: "remained-time", remained_time: { next_time_value: PrayTimeService.next_time(@pray_time).strftime("%Y-%m-%d %H:%M") } }}
      .text-white.text-6xl.font-medium
        = t(:remained_label, time: t("times.#{PrayTimeService.next_time_name(@pray_time)}", locale: :tr))
      .text-8xl.font-medium.text-amber-400{ data: { remained_time_target: "time" } } 00:00:00

  .col-span-12.h-screen.flex.flex-col.items-center.justify-around.py-4.md:col-span-5
    .flex.flex-col.items-center.justify-around.space-y-6.md:hidden{data: { controller: "remained-time", remained_time: { next_time_value: PrayTimeService.next_time(@pray_time).strftime("%Y-%m-%d %H:%M") } }}
      .text-white.text-2xl.font-medium
        = t(:remained_label, time: t("times.#{PrayTimeService.next_time_name(@pray_time)}", locale: :tr))
      .text-4xl.font-medium.text-amber-400{ data: { remained_time_target: "time" } } 00:00:00

    - PrayTimeService.times_as_array(@pray_time).each do |pray_time|
      = render PrayTimeComponent.new(label: pray_time[:label], times: pray_time[:times], is_active: pray_time[:is_active])
