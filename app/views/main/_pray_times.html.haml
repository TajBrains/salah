.flex.flex-col.items-center.justify-around.space-y-6{class: "md:hidden", data: { controller: "remained-time", remained_time: { next_time_value: @pray_time_service.next_time.strftime("%Y-%m-%d %H:%M") } }}
  .text-white.text-2xl.font-medium
    = t(:remained_label, time: t("times.#{@pray_time_service.next_time_name}", locale: :tr))
  .text-4xl.font-medium.text-amber-400{ data: { remained_time_target: "time" } } 00:00:00

- @pray_time_service.times_as_array.each do |pray_time|
  = render PrayTimeComponent.new(label: pray_time[:label], times: pray_time[:times], is_active: pray_time[:is_active])
